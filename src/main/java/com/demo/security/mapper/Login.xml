<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.security.dao.LoginDao" >

    <select id="loginUseMemId" parameterType="String" resultType="com.demo.member.vo.MemberVo">
    /* Login.xml - login */
        SELECT
            *
        FROM tb_member_basic
        WHERE mem_id = #{memId}
    </select>

    <select id="loginUseMemNo" parameterType="String" resultType="com.demo.member.vo.MemberVo">
    /* Login.xml - login */
        SELECT
            *
        FROM tb_member_basic
        WHERE mem_no = #{memNo}
    </select>

</mapper>