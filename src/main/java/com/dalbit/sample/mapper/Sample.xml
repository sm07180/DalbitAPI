<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dalbit.sample.dao.SampleDao" >

    <select id="getCount" resultType="int">
    /* Sample.xml - getCount */
        SELECT
            count(*)
        FROM tb_log_data
    </select>

    <select id="getList" resultType="SampleVo">
    /* Sample.xml - getList */
        SELECT
           *
        FROM tb_log_data
    </select>
    
    <insert id="insertSample" parameterType="SampleVo">
    /* Sample.xml - insertSample */
        INSERT INTO TEST(
          ID
          , NAME
          , AGE
        ) VALUES (
          #{id}
          , #{name}
          , #{age}
        )
    </insert>

    <select id="selectLogData" resultType="SampleVo">
    /* Sample.xml - selectLogData */
      SELECT
        idx
        , sp_name
        , data
        , upd_date
      FROM tb_log_data
    </select>

    <select id="selectMemId" resultType="SampleVo">
    /* Sample.xml - selectMemId */
        SELECT
            a.mem_id
            , a.mem_nick
            ,CONCAT(a.mem_id," ","(",a.mem_nick,")") id_info
            , IFNULL(SUM(b.ruby), 0) ruby
        FROM tb_member_basic a LEFT OUTER JOIN tb_member_broadcast_gift b ON a.mem_no = b.gifted_mem_no
        WHERE a.mem_slct = 'p'
        GROUP BY a.mem_id, a.mem_nick;
    </select>

</mapper>